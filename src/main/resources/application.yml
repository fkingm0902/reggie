server:
  port: 8080
spring:
  application:
    # 应用名称
    name: reggie

  shardingsphere:
    datasource:
      names: master,slave
      #主数据源
      master:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.jdbc.Driver
        url: jdbc:mysql://192.168.80.128:3306/reggie?characterEncoding=utf-8&useSSL=false&allowMultiQueries=true&serverTimezone=Asia/Shanghai
        username: root
        password: FHJz09020226@!
      #从数据源
      slave:
        type: com.alibaba.druid.pool.DruidDataSource
        driver-class-name: com.mysql.jdbc.Driver
        url: jdbc:mysql://192.168.80.129:3306/reggie?characterEncoding=utf-8&useSSL=false&allowMultiQueries=true&serverTimezone=Asia/Shanghai
        username: root
        password: FHJz09020226@!
    masterslave:
      # 读写分离配置
      load-balance-algorithm-type: round_robin
      # 最终数据源名称
      name: dataSource
      # 主数据库源名称
      master-data-source-name: master
      # 从数据库源名称
      slave-data-source-names: slave
    props:
      sql:
        show: true # 开启SQl显示 默认为false
  main:
    allow-bean-definition-overriding: true

  redis:
    host: 192.168.80.128
    port: 6379
    password: FHJz09020226@!
    database: 0
  cache:
    redis:
      time-to-live: 1800000 #设置缓存过期时间


  mail:
    host: smtp.qq.com
    #发送验证码的邮箱
    username: 860932016@qq.com
    # 你的QQ邮箱授权码
    password: uyciphgyhjwybfbc
mybatis-plus:
  configuration:
    #在映射实体或者属性时，将数据库中表名和字段名中的下划线去掉，按照驼峰命名法映射
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID

reggie:
  path: D:\reggie\


